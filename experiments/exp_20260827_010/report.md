# exp_20260827_010

Pre-registered zero-network successor to exp009. It does not rehabilitate exp009 and does not treat its reconstructed operator ledger as contemporaneous evidence.

The only inputs are the retained immutable v2 raw tree, the two externally bound manifests, and exact-hash vendored source. No v1 raw, Hub cache, network, Alpha, IC, P&L or backtest is authorized.

Checkpoint weights are retained for local verification only. Their redistribution permission has not been established.

The verifier reads the official HK/regression samples, so `real_data_accessed=true`; these are interface fixtures only and do not authorize an empirical Alpha claim. It rehashes the complete 19-file raw tree both before and after model checks and accepts only identical ordered evidence.

Development evidence: the first 9-test run retained 3 absolute-import errors; after safely shadowing and restoring `model` aliases, the final focused suite passed 11/11 and py_compile passed. No formal command ran. Network requests, retries, raw copies and downloads all remained zero.

## Formal execution and postflight

The frozen offline verifier was executed exactly once after Phase2 GO. It exited 0 in 3.3898401 seconds with empty stdout/stderr. A contemporaneous ledger was written and `formal_execution_count=1` was recorded before the result was inspected or any postflight test ran. There was no network request, retry, copy, or download.

The retained result is `experiments/exp_20260827_010/artifacts/offline_reverification_result.json` (2253 bytes, SHA-256 `71391fe41acad411ed22da08b4ad82691d993004d6704082c0203d2f1acadaf6`). It binds the 19-file, 562,430,552-byte raw tree, reports two identical complete verification passes, runs a finite two-row mini interface inference, and reproduces the eight-row official small regression fixture. This verifies supply-chain binding and the offline interface only.

The frozen targeted suite was executed once and passed 11/11. Full discovery was executed once: 661/664 passed; the three failures are unchanged lifecycle-state assertions in the older v2, v4, and v6 forward-schedule tests, whose consumed formal runs left reservation/final evidence that those tests still expect to be absent. They were neither changed nor hidden, and no exp010 test failed.

Terminal status remains `NEEDS_MORE_DATA`; artifact state is `SUPPLY_CHAIN_AND_OFFLINE_INTERFACE_VERIFIED`. `empirical_authorized=false`, historical eligibility remains unavailable, and this experiment provides no Alpha, IC, P&L, backtest, or model-quality conclusion. Checkpoint weight redistribution permission remains unestablished.

## Independent audit closure

Independent postflight review returned `FINAL PASS`. The append-only closure is `artifacts/independent_audit_closure.json` (1254 bytes, SHA-256 `bac546245766dd8a3bb075bff42251248b5bbddca466af727ff82202ca612233`). It was created after formal execution and does not alter the formal ledger, result, frozen bindings, or retained three lifecycle failures. Status is now `POSTFLIGHT_INDEPENDENT_AUDIT_PASSED`; the artifact and terminal ceilings remain `SUPPLY_CHAIN_AND_OFFLINE_INTERFACE_VERIFIED / NEEDS_MORE_DATA`.
